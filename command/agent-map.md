---
description: Build and render an agent node map/DAG for a goal before execution.
agent: orchestrator
---

Build an agent node map for this goal:

$ARGUMENTS

Use the `agent_graph` tool if available. Return:

- Recommended topology and why.
- Mermaid graph.
- Node table with agent, mission, inputs, outputs, dependencies, and verification signal.
- Parallel batches.
- Loop bounds and stop conditions.

Do not edit files. This is a planning/map command.
