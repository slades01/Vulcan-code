// VulcanCode — example configuration (0.0.0-dev-202606261805)
//
// This is a FROM-SCRATCH template with PLACEHOLDER values only. It intentionally
// contains no real keys. Copy to `opencode.jsonc`, then set the referenced
// environment variables in your shell. Never commit a real `opencode.jsonc`.
//
// VulcanCode launches via the `vulcan` command but uses the public opencode config
// schema under the hood. `$schema` points at that schema; adjust model ids and
// providers to match your runtime version.
{
  "$schema": "https://opencode.ai/config.json",

  // Default model for the orchestrator / top-level agent.
  "model": "openai/gpt-5.5-fast",
  // Default model for small/low-stakes work (titles, summaries).
  "small_model": "openai/gpt-5.4-mini",

  // Providers. Keys are read from the environment; never inline secrets here.
  "provider": {
    "openai": {
      "name": "OpenAI",
      "options": { "apiKey": "{env:OPENAI_API_KEY}" }
    },
    "anthropic": {
      "name": "Anthropic",
      "options": { "apiKey": "{env:ANTHROPIC_API_KEY}" }
    },
    "zai-coding-plan": {
      "name": "ZAI Coding Plan",
      "options": { "apiKey": "{env:ZAI_API_KEY}" },
      "models": {
        "glm-5.2": { "name": "GLM 5.2" }
      }
    }
  },

  // Global permission baseline. The shipped agents add their own (stricter)
  // frontmatter; `trusted-autonomy.ts` hardens any remaining `ask` into `deny`
  // for sensitive paths. See SECURITY.md and examples/permissions.md.
  "permission": {
    "edit": { "*": "ask" },
    "bash": { "*": "ask" },
    // Local/non-network workflow + code-graph tools. Allow them when the plugins
    // below are enabled. Code Memory writes the generated overlay into
    // <root>/.opencode/codemap (secrets redacted, never persisted). loop_guard,
    // pace_guard, synthesis, and agent_graph only render contracts/graphs;
    // recall_bus does read-only redacted retrieval; mission_state maintains the
    // non-secret mission run-state at <root>/.opencode/run/state.json.
    "code_memory": "allow",
    "codemap_health": "allow",
    "agent_graph": "allow",
    "loop_guard": "allow",
    "pace_guard": "allow",
    "synthesis": "allow",
    "recall_bus": "allow",
    "mission_state": "allow"
  },

  // Optional plugin tools. Uncomment to register the local VulcanCode workflow
  // and code-graph tools. Requires the plugin files under ./tools/. Add
  // "./plugins/metrics-tap.ts" to collect a non-secret <root>/.opencode/run/metrics.jsonl
  // event tail (used by loop_guard/pace_guard structured verdicts); it never
  // stores secrets.
  // "plugin": [
  //   "./tools/agent_graph.ts",
  //   "./tools/loop_guard.ts",
  //   "./tools/pace_guard.ts",
  //   "./tools/synthesis.ts",
  //   "./tools/mission_state.ts",
  //   "./tools/recall_bus.ts",
  //   "./tools/code_memory.ts",
  //   "./tools/codemap_health.ts",
  //   "./plugins/metrics-tap.ts"
  // ],

  // Optional MCP servers. Uncomment and configure as needed.
  // "mcp": {
  //   "playwright": {
  //     "type": "local",
  //     "command": ["npx", "@playwright/mcp@0.0.76"]
  //   }
  // }
}
